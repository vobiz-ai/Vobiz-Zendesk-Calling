{
  "name": "vobiz-zendesk-calling-backend",
  "version": "2.0.0",
  "description": "Calling backend for the Vobiz Zendesk CTI app. Holds the Vobiz account credentials, answers Vobiz webhooks, and writes calls back to Zendesk tickets.",
  "main": "server.js",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check zendeskService.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
